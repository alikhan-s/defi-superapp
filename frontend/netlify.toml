# Netlify SPA config (alternative to vercel.json). Client-side routing needs
# all paths to fall through to index.html.
[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
